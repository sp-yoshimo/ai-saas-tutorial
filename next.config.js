/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "img.clerk.com",
            "oaidalleapiprodscus.blob.core.windows.net"
        ]
    }
}

module.exports = nextConfig
